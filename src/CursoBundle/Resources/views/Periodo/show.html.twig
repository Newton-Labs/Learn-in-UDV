{% extends '::base_page.html.twig' %}

{% block body_content -%}
    <h1>Periodo</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombre Período</th>
                <td>{{ entity.nombrePeriodo }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('periodo') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('periodo_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
