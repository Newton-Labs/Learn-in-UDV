{% extends 'base_page_admin.html.twig' %}

{% block body_title%} <h1>Listado de Cursos</h1> {% endblock %}

{% block body_content -%}
   

    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre Curso</th>
                <th>Código Curso</th>
                <th>Estado</th>
                <th>Acciones</th>

            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href = "{{ path('curso_show', { 'slug': entity.slug }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombreCurso }}</td>
                {% for carrera in entity.carreras %}
                    <td> {{carrera}} </td>
                    
                {% endfor %}

                {% if entity.deletedAt != null %}
                    <td>Eliminado {{entity.deletedAt | date('d-m-Y H:i:s') }}</td>
                {% else %}
                    <td>Activo</td>
                    P
               
                <td>
                <ul>
                    <li>
                        <a href="{{ path('curso_show', { 'slug': entity.slug }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('curso_edit', { 'slug': entity.slug }) }}">edit</a>
                    </li>
                </ul>
                </td>
                 {% endif %}
            </tr>
        {% endfor %}
        </tbody>
    </table>
<ul>
        <li>
            <a href="{{ path('curso_new') }}">
                Crear Nuevo Curso
            </a>
        </li>
    </ul>
    {% endblock %}

