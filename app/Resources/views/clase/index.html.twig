{% extends 'base_page.html.twig' %}

{% block body_content %}
    <h1>Listado de Clases</h1>

    <table>
        <thead>
            <tr>
                <th>Área</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for clase in clases %}
            <tr>
                <td><a href="{{ path('clase_show', { 'id': clase.id }) }}">{{ clase.id }}</a></td>
                <td>{{ clase.area }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('clase_show', { 'id': clase.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('clase_edit', { 'id': clase.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a btn="btn btn-primary" href="{{ path('clase_new') }}">Create nueva clase</a>
        </li>
    </ul>
{% endblock %}
